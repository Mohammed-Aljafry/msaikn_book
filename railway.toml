[build]
builder = "nixpacks"
buildCommand = "python -m pip install -r requirements.txt"

[deploy]
startCommand = "daphne src.msakin.asgi:application --port $PORT --bind 0.0.0.0"
healthcheckPath = "/"
healthcheckTimeout = 100
restartPolicyType = "on-failure"

[env]
PYTHON_VERSION = "3.11.7"
